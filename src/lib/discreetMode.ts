// Discreet Mode utilities for UI state management

// CSS classes for discreet mode interactions (kept for backwards compatibility if needed)
export const getDiscreetClasses = (isDiscreet: boolean): string => {
  // This function is now deprecated - use DiscreetText component instead
  return ''
}
